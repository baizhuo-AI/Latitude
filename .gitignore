node_modules
dist
coverage
.DS_Store
src-tauri/target

# Superpowers skill 工具目录(brainstorm 工作区等),非项目代码
.superpowers/

# Claude Code 本机私有配置(权限白名单、launch 配置,含本机路径)
.claude/

# 环境变量(包含明文 API key,绝不进 git)
.env
.env.local
.env.*.local

# 本地 SQLite 数据库(开发期可能在这里)
*.db
*.db-journal

# ==== pipeline/ 采集转写管线（整目录不进主仓）====
# 定位：本地实验资产或独立私有仓，见 pipeline/README.md 的边界声明。
# 保留下面的细化规则作为双保险（即便将来放开目录，数据仍绝不入库）。
pipeline/
# 凭据(明文 API key)
pipeline/.env
# 虚拟环境
pipeline/.venv/
pipeline/__pycache__/
# 音频原件与转写产物(含真实工作对话,绝不进 git)
pipeline/audio/
pipeline/out/
*.wav
*.WAV
*.mp3
# 热词表 id(非密钥,但属本机运行态)
pipeline/.vocab_id
